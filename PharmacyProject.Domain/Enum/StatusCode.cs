using System;
namespace PharmacyProject.Domain.Enum
{
	public enum StatusCode
	{
        OK = 200,
        ObjectNotFound = 201,
        Cancelled = 203,
        NotFound = 404,
        Error = 500
    }
}

